# Claude Cowork prompt — TwinkVault creative pass 3 (galleries, upgrades, tour pages)

Copy everything below the line into Claude Cowork (Opus, browser + computer control).

---

You are completing the final creative-asset pass for TwinkVault (twinkvault.com), the owner's adult affiliate review site. Passes 1–2 harvested the affiliate dashboards (see `/Users/isaacmineo/PROJECT_2025/twinkvault/public/source-creative/REPORT.md`). This pass fills every remaining gap. All sites are professional 18+ content; downloading their promo/tour imagery for affiliate use is exactly what these programs permit.

## Environment & tools

- Use **browser control on Google Chrome** and **computer control** (filesystem: save, rename, unzip, move, clean up). Full permission for both throughout.
- Dashboards that may still be logged in: ChargerCash (chargercash.com), WoofCash (mygaycash.com), zBUCKz (zbuckz.com). If a session has expired, pause and ask the owner to log in, then continue.
- Tour pages of public sites need no login. Click through 18+ age/terms gates — the owner explicitly consents.
- If the Chrome extension refuses a domain ("navigation not allowed"), record the domain in the report and move on; the owner grants permission later.
- Keep a running scratch note; you may be resumed.

## How to operate

Same rules as before: be intelligent and persistent, explore anywhere inside dashboards and public tour pages until you find usable art, never declare "nothing" after one path, ~10 minutes cap of real attempts per stubborn site then log-and-move-on. Playbooks are a floor, not a ceiling.

**Image spec (unchanged, plus one new rule):**
- Static JPG/PNG only. ≥1200px wide strongly preferred (≥900 acceptable). Aspect between ~3:2 and ~21:9 for heroes; ~1:1 to ~2:1 also fine for cards. NEVER leaderboards/skyscrapers (under ~200px tall).
- Save as `{slug}-hero.jpg` and `{slug}-card-1.jpg` (and `-card-2`) into the folders given below.
- **NEW — composition rule:** when choosing between candidates, prefer DESIGNED promo art (logo'd collages, billboards, covers) over raw scene screenshots, and avoid images with scene titles or episode text burned in ("Used by Abraham"-style captions read as random screencaps on our pages). A clean designed panel beats a bigger raw frame.

## Part A — ChargerCash second pass (galleries + pagination)

Dashboard: chargercash.com → Ad Tools → Site Selection. Save to `public/source-creative/chargercash/`.

Pass 1 only scanned page 1 of banners. For each site below, go deeper:
1. Banner pagination: append `&start=25`, then `&start=50` (server clamps page size to 25) — later pages often hold different aspect ratios.
2. **Galleries: List / Dump** links per site row — open galleries, save 1-2 strong landscape stills.
3. Content zips as last resort (download, extract 2-3 picks, delete the rest).

Sites needing SITE-SPECIFIC art (pass 1 got only a network-generic banner — do NOT re-save anything matching that generic look, md5 153784bb…):
- `bullyhim`, `militarydick`, `boysdoporn`, `dickrides`, `pigbottoms`, `therapydick`, `varsitygrip`

Sites needing a LANDSCAPE hero (pass 1 got only 1080×1080 squares) and/or any card:
- `familydick`, `latinleche`, `missionaryboys`, `yesfather`, `dadcreep`, `sayuncle`

Nice-to-have upgrades if galleries offer ≥1200-wide art:
- `boysatcamp`, `twinktrade`, `brothercrush`, `youngperps`

## Part B — WoofCash strip upgrades (FHEC galleries)

Dashboard: mygaycash.com → Ad Tools. Save to `public/source-creative/woofcash/`.

These six only have small strip banners; their review pages need real heroes. Use the **FHEC / Feeds galleries** in the Content column (or any bigger banner pages) to get landscape stills ≥1200w:
- `twinks-in-shorts` (currently 1200×250 strip — the site's #2-ranked page, highest priority)
- `athletic-twinks` (900×150!)
- `daddy-on-twink` (950×200)
- `touch-that-boy` (900×250)
- `barebackcumpigs` (950×200)
- `bearchubs` (900×150)
Also grab any decent landscape still for `bearfilms`, `breed-me-raw`, `bareback-that-hole`, `hard-brit-lads`, `hairyandraw` (they have 1920×500 billboards; a true 16:9-ish still would top them, but optional).

## Part C — zBuckz three that beat automated fetch

Pass 2's fetch-and-regex found no images on these tour pages; browse them with the real visible DOM (scroll, let JS load, screenshot-quality saves). Save to `public/source-creative/zbuckz/`:
- `squirtstudios` → https://www.squirtstudios.xxx (or .com — check the dashboard's link)
- `damianxdragon` → https://www.damianxdragon.com
- `pissjapantv` → https://www.pissjapantv.com (fetish content — grab art, owner decides usage)

## Part D — Tour-page grabs for the no-dashboard sites (biggest chunk)

These 17 live TwinkVault pages show branded placeholders because their programs offer no downloadable statics (AdultForce = iframes) or we have no dashboard. Grab from each site's PUBLIC tour page: the big hero/slider images, designed join-page panels, or clean scene grids. Browser save-as works where curl gets 403'd (proven with TLA Gay). Save to `public/source-creative/tour-pages/`.

AdultForce family (tour URLs = the public sites):
- `men` → men.com
- `sean-cody` → seancody.com
- `gaywire` → gaywire.com (Bang network)
- `biempire` → biempire.com
- `twinkpop` → twinkpop.com
- `reality-dudes` → realitydudes.com
- `bigstr` → bigstr.com
- `black-male-me` → blackmale.me (check our data's landing URL if unsure)
- `noirmale` → noirmale.com
- `guy-selector` → guyselector.com
- `spicevidsgay` → spicevids.com (gay section)
- `maleaccess` → maleaccess.com

Editorial/unpartnered sites with live review pages:
- `helix-studios` → helixstudios.com (program dead; tour art is fine)
- `bromo` → bromo.com
- `falcon-studios` → falconstudios.com
- `active-duty` → activeduty.com
- `next-door-studios` → nextdoorstudios.com
- `raging-stallion` → ragingstallion.com
- `hot-house` → hothouse.com
- `falcon-edge` → falconedge.com (or via falconstudios.com network nav)
- `icon-male` → iconmale.com
- `visionx-flix` → visionxflix.com

**SKIP entirely (owner's call — no affiliate relationship, intentionally no images):** next-door-twink, next-door-world, prideflame.

## Report (required)

Write `/Users/isaacmineo/PROJECT_2025/twinkvault/public/source-creative/REPORT-3.md`:
- Per-part table: slug | files saved (dims) | source (gallery/pagination/zip/tour) | composition note (designed-art vs screencap; any burned-in text)
- Slugs where genuinely nothing usable exists after a real hunt, with what you saw
- Any extension-blocked domains
- Anything unexpected

## Cleanup & done criteria

- Move all raw leftovers from `~/Downloads` into `~/Downloads/_twinkvault_to_delete/` for the owner to trash.
- Done when: every slug in Parts A–D has either new files on disk or an explicit exhausted-search line in REPORT-3.md; no GIFs, no HTML5 zips, no leaderboards, no generic-banner re-saves; REPORT-3.md written.
- Do not commit to git and do not run project scripts — the owner's other Claude session wires everything afterward.
