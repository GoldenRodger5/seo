# Claude Cowork prompt — TwinkVault creative pass 4 (the finish line)

Copy everything below the line into Claude Cowork (Opus, browser + computer control).

---

You are finishing the TwinkVault creative-asset project. Passes 1–3 delivered ~90 files; this pass collects the final stragglers. Reports from earlier passes live at `/Users/isaacmineo/PROJECT_2025/twinkvault/public/source-creative/REPORT.md` and `REPORT-3.md` — read them first for context on what failed and why. All sites are professional 18+ content; this imagery is provided for exactly this affiliate use.

## Ask for help — this is a rule, not a suggestion

Several items below failed in earlier passes for environment reasons (extension permissions, download quotas, CORS). When you hit a blocker you cannot work around in ~5 minutes, STOP and ASK THE OWNER — in chat, specifically: "I need X — can you do Y?" (grant a domain in the extension's site-access settings, log into a dashboard, right-click-save one image manually, restart the browser session). The owner is available and prefers a 30-second interruption over a skipped item. Never silently give up; either finish an item, get help and finish it, or record exactly why it is impossible.

## Environment & tools

- **Browser control on Google Chrome** + **computer control** (filesystem). Full permission for both.
- Before starting, ASK the owner to grant the claude-in-chrome extension access to these domains (they blocked pass 3): twinkpop.com, bigstr.com, blackmale.me, guyselector.com, helixstudios.com, ragingstallion.com, hothouse.com, iconmale.com, falconedge.com, pissjapantv.com, dadcreep.com. Wait for confirmation, then proceed.
- Age/terms gates on tour pages: click through — owner consents.
- Keep a scratch note; you may be resumed.

## Image spec (same as always)

Static JPG/PNG. ≥1200px wide preferred (≥900 acceptable). Heroes ~3:2 to ~21:9 (designed wide billboards up to ~4:1 are FINE — the site renders them uncropped); cards ~1:1 to ~2:1. No leaderboards under ~200px tall, no GIFs, no HTML5 zips. Prefer DESIGNED promo art (logos, collages, billboards) over raw screencaps; avoid burned-in scene-title text. Save as `{slug}-hero.jpg` / `{slug}-card-1.jpg`.

## The list (12 items — everything else is done)

Save tour-page grabs to `public/source-creative/tour-pages/`, dashboard grabs to their program folder.

**A. Extension-permission unblocks (tour pages, after the owner grants):**
1. `twinkpop` → twinkpop.com
2. `bigstr` → bigstr.com
3. `black-male-me` → blackmale.me
4. `guy-selector` → guyselector.com
5. `helix-studios` → helixstudios.com (flagship — the #1-ranked site still has NO art; prioritize)
6. `raging-stallion` → ragingstallion.com
7. `hot-house` → hothouse.com
8. `icon-male` → iconmale.com
9. `falcon-edge` → falconedge.com (or via falconstudios.com network nav)
10. `pissjapantv` → pissjapantv.com (fetish — grab, owner decides usage)

**B. The gammacdn four (CORS-tainted for automation — do these WITH the owner):**
- `active-duty` → activeduty.com, `falcon-studios` → falconstudios.com, plus a card for `next-door-studios` and anything better for `falcon-edge`.
- Technique: navigate to the tour page, then ASK THE OWNER to right-click → Save Image As… on the big hero/carousel panels you point out (gammacdn blocks fetch and taints canvas; a human save-as works). Tell them the exact filename to save it under, or rename after they save to Downloads.

**C. WoofCash stragglers (dashboard: mygaycash.com — ask owner to log in if expired):**
- `daddy-on-twink` and `touch-that-boy`: the 1920×1080 FHEC art was already LOCATED in pass 3 (see REPORT-3.md for the exact FHEC URLs) but Chrome's download quota blocked the save. In this fresh session, re-open those FHEC scenes and save the promo stills.
- `athletic-twinks` and `bearchubs`: no FHEC; use the dashboard's per-scene ZIP download (Content column → scene → ZIP icon), extract, pick 1-2 landscape stills ≥1200w, delete the rest.

**D. One retry:**
- `reality-dudes` → realitydudes.com — pass 3 only found 600×337 posters. Scroll the tour properly, let carousels load; if a real ≥1200 hero genuinely doesn't exist, record that and move on.

## Report

Write `public/source-creative/REPORT-4.md`: per-slug table (files, dims, source, composition note), every ask-for-help moment and its outcome, anything still impossible with the precise reason. Move Download leftovers into `~/Downloads/_twinkvault_to_delete/`.

## Done criteria

Every slug above has files on disk OR a documented impossibility. That completes the entire image project — after this, every live TwinkVault page has real art or an intentional placeholder, and the owner's other Claude session wires these final files. Do not commit to git; do not run project scripts.
